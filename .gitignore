# secrets — 永不提交
worker/.dev.vars
.env
*.env

# 真实部署配置（含域名 / Feishu / D1 的真值）—— 开源仓库只提供 .example 模板
# fork 者：cp worker/wrangler.toml.example worker/wrangler.toml
worker/wrangler.toml

# 私密素材（含手机号/薪资），不进开源仓库
resume-base-php-detail.md

# 完整简历 PDF（全部履历）—— 本地生成、随部署上线，但不进公开仓库
# 否则任何人可从 GitHub raw 直接下载，绕过 send_resume 的邮箱门控
# （brief.pdf 是人人可得的一页简介，保留作模板示例）
worker/assets/resume.pdf
worker/assets/resume-en.pdf
# 简历的 HTML 源（同为完整履历，能直接渲染成 PDF）也不进公开仓库；brief.html 保留作示例
pdf-src/resume.html
pdf-src/resume-en.html

# 本地 AI 工具产物 / vendored skill（不进开源仓库）
.seek/
worker/.seek/
skills-lock.json
.agents/
.junie/
.claude/skills/design-taste-frontend/

# tooling
.wrangler/
node_modules/
.DS_Store
